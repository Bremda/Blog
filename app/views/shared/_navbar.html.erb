<nav class="navbar" style="background-color: #E8E7EF;" role="navigation" aria-label="main navigation">
  <div class="container">
    <div class="navbar-brand">
      <img class="rounded-circle" alt="cat icon" src="/assets/cat.jpg" width="30">
      <%= link_to 'Home', root_path, class: 'btn' %>
    </div>
    <div class="justify-content-end">
      <% if user_signed_in? %>
        <a href="/posts/new" class="btn" tabindex="-1" role="button" aria-disabled="true">New Post</a>
      <% end %>
      <%= link_to "About Me", pages_about_me_path, class:"btn" %>
      <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, "data-turbo-method" => :delete, class: "btn" %>
      <% else %>
        <a class="btn btn-dark" href='/users/sign_in' tabindex="-1" role="button" aria-disabled="true">
          Sign In
        </a>
      <% end %>
    </div>
  </div>
</nav>
